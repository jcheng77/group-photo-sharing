<h1>Listing photos</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @photos.each do |photo| %>
  <tr><%= image_tag photo.file.url(:medium) %></tr>
<% end %>
</table>

<br />

<%= link_to 'New Photo', new_photo_path %>
