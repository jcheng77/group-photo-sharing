<h1>Listing albums</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @albums.each do |album| %>
  <tr>
    <td> <%= album.title %> </td>
  <td>  <%= link_to image_tag(album.cover.file.url(:medium)), album %> </td>
  <td> <%= link_to 'Add Photos', new_album_photo_path(album) %> </td>
<% end %>
</table>

<br />

<%= link_to 'New Album', new_album_path %>
